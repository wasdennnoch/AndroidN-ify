<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
        android:color="@color/system_primary_color">
    <!-- We don't want a ripple, but the SystemUI crashes when there is no
     ripple, so just hide it by making it the same color as the background -->
    <item android:drawable="@color/system_primary_color"/>
</ripple>